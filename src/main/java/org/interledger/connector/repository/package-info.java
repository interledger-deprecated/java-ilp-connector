/**
 * Contains interfaces that define optional data storage for persistent operations such as
 * correlating a source and destination transfer in order to process various events.
 */
package org.interledger.connector.repository;